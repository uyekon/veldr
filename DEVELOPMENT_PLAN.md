# 个人 WebApp 与跨平台笔记 App 开发计划

## 产品目标与架构

近期继续完善个人 WebApp；长期在同一后端基础上推出 iOS，再扩展 Android 和订阅同步。

- Web：通过 API 读写服务器，不使用 IndexedDB 保存业务数据。
- iOS / Android：无需登录即可本地使用；登录且订阅有效后开启云端同步。
- 手机本地保存不依赖网络或订阅状态。
- 不加入工作回顾、协作空间等功能。
- 个人自用阶段不要求引入订阅限制。

## 估算前提

以下时间按 **1 名有全栈及移动端经验的开发者全职开发，结合 AI 辅助**估算，包含基本测试和修复。兼职开发通常需要延长至约 2–3 倍日历时间。

**iOS 首个可订阅版本预计 15–23 周；三端基本完整预计 18–28 周。** 排期建议额外预留约 20% 缓冲。App Store 审核、账号开通和可能的补充材料会影响日历时间，不能保证某天通过。

## 分阶段计划

| 阶段 | 实现内容 | 阶段目标与验收标准 | 预计时间 |
|---|---|---|---|
| 0. 梳理现有项目 | 审查保存、转文章、归档、筛选；整理数据结构和接口；建立测试环境、发布与回滚流程 | 核心流程无已知阻断问题；发布产物与提交一致；能够回滚 | 1 周 |
| 1. 完善个人 WebApp | 修复自动保存竞态和转换重复提交；增加备份、恢复、导出；改善移动浏览器交互 | 连续使用稳定；保存失败有提示；转文章不丢内容；备份可实际恢复 | 2–3 周 |
| 2. 建立跨平台数据基础 | 将 CMS JSON 存储迁移到关系数据库；引入全局唯一 ID、用户归属、版本号、删除标记；明确 API 契约 | 现有数据完整迁移；Web 功能保持正常；新结构支持多设备和多用户 | 2–3 周 |
| 3. 开发 iOS 本地版 | 本地数据库、笔记编辑、日记、Notebook、分类、标签、搜索、置顶、归档、附件、导入导出 | 无需登录；飞行模式下完整使用；关闭及重启 App 后内容完整保留 | 4–6 周 |
| 4. 账号与跨设备同步 | 独立账号、数据隔离；增量同步、首次合并、附件同步、冲突处理、删除同步；接入 Web 数据 | iOS 与 Web 双向同步；断网重试不重复；冲突不静默丢失内容 | 4–6 周 |
| 5. 订阅与 iOS 上架 | Apple 应用内订阅、服务端权益校验、恢复购买、到期处理；隐私说明、账号删除；TestFlight 测试和提审 | 购买、续费、恢复、到期流程完整；到期仍可本地使用与导出；提交可审核版本 | 2–4 周 |
| 6. Android 版本 | 复用数据协议和同步逻辑；适配本地存储、文件、后台行为；按目标渠道接入订阅 | Android、iOS、Web 三端同步一致；移动端离线能力一致 | 3–5 周 |

## 技术路线建议

考虑同时支持 iOS 和 Android，建议采用 **Flutter 作为移动端客户端，继续保留现有 Web 前端和 Node.js 后端**。移动端共用大部分界面与业务代码，平台支付和文件能力分别适配。

阶段 0 先用一个小原型验证 Markdown 编辑、长文输入和中文输入法体验，再锁定技术路线；编辑体验不合格会显著影响后续工期。

## 各阶段重点

### 阶段 1：个人 WebApp 可靠性

- 自动保存请求期间继续输入，不能被旧响应覆盖，也不能错误显示“已保存”。
- 转文章需要校验白板版本，防止把其他设备刚修改的内容误清空。
- 连续点击、请求超时后重试，不能创建重复文章。
- “保存文章＋清空白板”需要数据库事务支持，在阶段 2 数据库迁移时完成事务落地。
- 发布时从确定的提交重新构建，并保留上一版本及数据库备份。

### 阶段 2：数据基础与兼容

不要求立即开放公众注册。先把现有数据归属到个人账号，保持当前使用方式，等移动端同步阶段再完善注册、找回账号和账号删除。

旧数字 ID 应保留映射，避免现有文章链接失效。

### 阶段 3：移动端本地能力

先保证 Markdown 文本和图片体验。复杂富文本、视频编辑、多人协作暂不进入首版。

日记保存及转文章必须完全在本地完成；未订阅用户也能正常使用这些功能。

### 阶段 4：同步验收

同步是整个项目技术风险最高的部分，应覆盖以下场景：

| 场景 | 预期结果 |
|---|---|
| 手机离线修改，Web 同时修改同一篇文章 | 保留双方修改，提供冲突处理入口 |
| 首次订阅，本地和云端都有笔记 | 合并导入，不自动覆盖或清空 |
| 上传成功但响应丢失，手机重试 | 不重复创建笔记或附件 |
| 一台设备删除，另一台设备仍有旧副本 | 不因旧设备上线而自动复活 |
| 同步中退出账号或切换账号 | 不发生跨账号上传或显示 |
| 订阅到期 | 暂停云端同步，本地编辑和导出继续可用 |

### 阶段 5：订阅边界

订阅到期后的云端数据保留期限、Web 访问权限，需要在对外销售前明确。

开发阶段建议采用保守默认：不因订阅到期立即删除云端数据，也不限制用户读取、导出已有内容；个人账号保留完整访问能力。

## 交付里程碑

每个阶段都交付可以独立使用的结果：

- **约第 3–4 周**：更可靠的个人 WebApp。
- **约第 9–13 周**：无需登录的 iOS 本地版，可开始 TestFlight 自用。
- **约第 13–19 周**：iOS 与 Web 同步测试版。
- **约第 15–23 周**：具备订阅能力的 iOS 提审版本。
- **之后 3–5 周**：Android 版本。

## 近期执行建议

先安排阶段 0 和阶段 1，共 3–4 周。这部分直接改善日常使用，同时建立可靠的保存、备份和发布流程，后续开发移动端时继续复用。

## 阶段一实现进展

已实现保存队列、日记转换版本与去重保护、串行文件提交、备份校验与恢复脚本、文章 Markdown/ZIP 导出、转换弹窗分类联动、固定提交构建和发布回滚工具。

自动化验收覆盖后端故障场景、隔离目录恢复与回滚，以及桌面/移动视口浏览器流程。生产 JSON 备份 timer 已于 2026-09-17 启用并验证，生产数据副本也已完成隔离恢复；真机 Safari/Chrome 仍待人工验收。

操作步骤、接口变化和验收说明见 [阶段一交付与运维手册](deploy/PHASE_ONE.md)。

## 阶段二实现进展

已建立 PostgreSQL 关系模型、JSON 兼容存储适配层、UUID/用户归属/版本/软删除、幂等变更记录，以及供移动端使用的 bootstrap 与游标增量接口。现有 Web 继续使用 `/api/cms`，无需一次性迁移路由或 IndexedDB。

已提供 JSON 迁移与反向导出、PostgreSQL+附件备份、restic 异地复制和隔离恢复工具。本地 PostgreSQL 已完成真实导入、旧接口兼容、v1 冲突/幂等/墓碑与数据库恢复测试。

生产数据副本已完成 PostgreSQL 迁移、备份、隔离恢复和反向导出，60 篇文章、17 个附件及关联数据的数量与内容摘要一致。正式切换前仍须提供异地备份服务器 SSH 参数，并完成移动浏览器人工验收。详细步骤见 [阶段二迁移与运维手册](deploy/PHASE_TWO.md)。
